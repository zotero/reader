module.exports = {
  plugins: {
    'autoprefixer': {},
    'postcss-rtl': {
      // onlyDirection: 'rtl',
      // addPrefixToSelector(selector, prefix) {
      //   return (prefix === '[dir]' ? '' : (prefix + ' ')) + selector;
      // }
    }
  }
}
